name: CI
on: [push, pull_request]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        ruby: ["2.5", "2.6", "2.7", "3.0", "3.1"]
        bundler: [default]
        gemfile:
          - rails5.1
          - rails5.2
          - rails6.0
          - rails6.1
          - stripe3
          - stripe4
          - stripe5
        include:
          - { ruby: "2.3", gemfile: "rails3.2", bundler: "1" }
          - { ruby: "2.3", gemfile: "rails4.2", bundler: "1" }
          - { ruby: "2.4", gemfile: "rails4.2", bundler: "1" }
        exclude:
          - { ruby: "2.5", gemfile: "rails7.0" }
          - { ruby: "2.6", gemfile: "rails7.0" }
          - { ruby: "3.0", gemfile: "rails5.1" }
          - { ruby: "3.0", gemfile: "rails5.2" }
          - { ruby: "3.0", gemfile: "stripe3" }
          - { ruby: "3.0", gemfile: "stripe4" }
          - { ruby: "3.1", gemfile: "rails5.1" }
          - { ruby: "3.1", gemfile: "rails5.2" }
          - { ruby: "3.1", gemfile: "rails6.0" }
          - { ruby: "3.1", gemfile: "stripe3" }
          - { ruby: "3.1", gemfile: "stripe4" }
    env:
      BUNDLE_GEMFILE: gemfiles/${{ matrix.gemfile }}.gemfile
    steps:
      # https://github.com/marketplace/actions/checkout
      - uses: actions/checkout@v2
      # https://github.com/marketplace/actions/setup-ruby-jruby-and-truffleruby
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby }}
          bundler: ${{ matrix.bundler }}
          bundler-cache: true # runs `bundle install` and caches gems automatically
      - name: Run tests
        run: bundle exec rake
